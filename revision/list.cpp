#include<bits/stdc++.h>
using namespace std;


int main()
{
    list<int> ls;
    ls.push_back(4);
    ls.emplace_back(8);
    //  push_font is cheper in compair to timecomplex
    ls.push_front(8);
    ls.emplace_front(5);


    // baki fxn are same to vector
    return 0;

}